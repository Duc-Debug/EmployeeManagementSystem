spring.application.name=employeemanagement
server.port=${SERVER_PORT:8080}

# Optional .env loading for development/deployment environments
spring.config.import=optional:file:.env[.properties],optional:file:../.env[.properties]

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_FORMAT_SQL:false}
spring.jpa.open-in-view=false

# JWT common configuration
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Safe default: do not bootstrap admin unless explicitly enabled
app.security.initial-admin.enabled=${INITIAL_ADMIN_ENABLED:false}